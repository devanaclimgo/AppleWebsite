import { appleImg } from '../utils'
import React from 'react'

const Navbar = () => {
  return (
    <header>
      <nav>
        <img src={appleImg} />
      </nav>
    </header>
  )
}

export default Navbar